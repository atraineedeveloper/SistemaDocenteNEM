<Window x:Class="SistemaDocente.App.Wpf.EditarEvaluacionCeldaWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:controls="clr-namespace:SistemaDocente.App.Wpf.Controls"
        xml:lang="es-MX"
        Title="Editar evaluación"
        Width="580" Height="520" MinWidth="520" MinHeight="460"
        ResizeMode="CanResizeWithGrip"
        WindowStartupLocation="CenterOwner"
        ShowInTaskbar="False"
        Background="{DynamicResource WindowBackgroundBrush}">

    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="Styles/PopupStyles.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Style="{StaticResource PopupHeader}">
            <StackPanel>
                <TextBlock x:Name="ActividadText" Style="{StaticResource PopupEyebrow}"/>
                <TextBlock x:Name="EstudianteText" Style="{StaticResource PopupTitle}" Margin="0,4,0,4"/>
                <TextBlock Text="Selecciona el resultado y agrega una observación cuando aporte información pedagógica útil."
                           Style="{StaticResource PopupSubtitle}"/>
            </StackPanel>
        </Border>

        <ScrollViewer Grid.Row="1"
                      VerticalScrollBarVisibility="Auto"
                      HorizontalScrollBarVisibility="Disabled"
                      Padding="24,20">
            <Border Style="{StaticResource PopupCard}">
                <StackPanel>
                    <TextBlock Text="Resultado de la actividad" Style="{StaticResource PopupSectionTitle}"/>
                    <TextBlock Text="El sistema actualiza automáticamente el estado de entrega según el resultado elegido."
                               Style="{StaticResource PopupSubtitle}" Margin="0,0,0,14"/>

                    <controls:FormField Header="Resultado *">
                        <ComboBox ItemsSource="{Binding OpcionesResultado}"
                                  DisplayMemberPath="Texto"
                                  SelectedValuePath="Resultado"
                                  SelectedValue="{Binding Resultado, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                  MinHeight="38"
                                  Margin="0,4,0,10"
                                  AutomationProperties.Name="Resultado de la evaluación"
                                  TabIndex="1"/>
                    </controls:FormField>
                    <TextBlock Text="“Entregada · evaluar después” conserva la entrega sin asignar todavía un nivel de logro."
                               Style="{StaticResource PopupSubtitle}" Margin="0,-6,0,14"/>

                    <controls:FormField Header="Observación pedagógica">
                        <TextBox Text="{Binding Observacion, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                 AcceptsReturn="True"
                                 TextWrapping="Wrap"
                                 VerticalScrollBarVisibility="Auto"
                                 MaxLength="500"
                                 MinHeight="170"
                                 VerticalContentAlignment="Top"
                                 AutomationProperties.Name="Observación de la evaluación"
                                 TabIndex="2"/>
                    </controls:FormField>

                    <TextBlock Text="Máximo 500 caracteres. Describe evidencia observable o retroalimentación útil."
                               Style="{StaticResource PopupSubtitle}"/>
                </StackPanel>
            </Border>
        </ScrollViewer>

        <Border Grid.Row="2" Style="{StaticResource PopupFooter}">
            <Grid>
                <TextBlock Text="Esc para cancelar"
                           Style="{StaticResource PopupSubtitle}"
                           VerticalAlignment="Center"/>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button Content="Cancelar"
                            IsCancel="True"
                            Click="OnCancelar"
                            MinWidth="100"
                            TabIndex="3"/>
                    <Button Style="{StaticResource PrimaryButton}"
                            Content="Aplicar a la matriz"
                            IsDefault="True"
                            Click="OnAceptar"
                            MinWidth="150"
                            TabIndex="4"/>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</Window>