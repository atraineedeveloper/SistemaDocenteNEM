<Window x:Class="SistemaDocente.App.Wpf.DialogoMensajeWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xml:lang="es-MX"
        Title="{Binding Titulo}" Height="220" Width="420" MinHeight="200" MinWidth="340"
        WindowStartupLocation="CenterOwner" ShowInTaskbar="False" ResizeMode="NoResize"
        Background="{DynamicResource WindowBackgroundBrush}"
        AutomationProperties.LiveSetting="Polite"
        Opacity="0">
    <Window.RenderTransform>
        <ScaleTransform ScaleX="0.96" ScaleY="0.96" CenterX="0" CenterY="0"/>
    </Window.RenderTransform>
    <Window.Triggers>
        <EventTrigger RoutedEvent="Loaded">
            <BeginStoryboard>
                <Storyboard>
                    <DoubleAnimation Storyboard.TargetProperty="Opacity" From="0" To="1" Duration="0:0:0.18"/>
                    <DoubleAnimation Storyboard.TargetProperty="RenderTransform.ScaleX" From="0.96" To="1" Duration="0:0:0.18">
                        <DoubleAnimation.EasingFunction>
                            <CubicEase EasingMode="EaseOut"/>
                        </DoubleAnimation.EasingFunction>
                    </DoubleAnimation>
                    <DoubleAnimation Storyboard.TargetProperty="RenderTransform.ScaleY" From="0.96" To="1" Duration="0:0:0.18">
                        <DoubleAnimation.EasingFunction>
                            <CubicEase EasingMode="EaseOut"/>
                        </DoubleAnimation.EasingFunction>
                    </DoubleAnimation>
                </Storyboard>
            </BeginStoryboard>
        </EventTrigger>
    </Window.Triggers>
    <Grid Margin="{DynamicResource MarginXXXLarge}">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <DockPanel Grid.Row="0">
            <Border x:Name="IconBorder" DockPanel.Dock="Left" Width="48" Height="48" CornerRadius="24" Margin="0,0,16,0"
                    Background="{DynamicResource InfoBackgroundBrush}" VerticalAlignment="Top">
                <TextBlock x:Name="IconText" FontSize="24" HorizontalAlignment="Center" VerticalAlignment="Center"/>
            </Border>
            <StackPanel VerticalAlignment="Center">
                <TextBlock Text="{Binding Titulo}" Style="{StaticResource Heading2}" Margin="0,0,0,8"/>
                <TextBlock Text="{Binding Mensaje}" Style="{StaticResource Body}" TextWrapping="Wrap"/>
            </StackPanel>
        </DockPanel>

        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,16,0,0">
            <Button x:Name="BtnAfirmativo" Content="Sí" IsDefault="True" Click="OnAfirmativoClick" Margin="4,0"/>
            <Button x:Name="BtnNegativo" Content="No" Click="OnNegativoClick" Margin="4,0"/>
            <Button x:Name="BtnCancelar" Content="Cancelar" IsCancel="True" Click="OnCancelarClick" Margin="4,0"/>
        </StackPanel>
    </Grid>
</Window>
