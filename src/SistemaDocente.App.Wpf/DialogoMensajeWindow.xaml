<Window x:Class="SistemaDocente.App.Wpf.DialogoMensajeWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xml:lang="es-MX"
        Title="{Binding Titulo}"
        Width="480" MinWidth="420" MinHeight="230" MaxHeight="520"
        SizeToContent="Height"
        WindowStartupLocation="CenterOwner"
        ShowInTaskbar="False"
        ResizeMode="NoResize"
        Background="{DynamicResource WindowBackgroundBrush}"
        AutomationProperties.LiveSetting="Polite">

    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="Styles/PopupStyles.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Border x:Name="RootBorder"
                Grid.Row="0"
                Style="{StaticResource PopupCard}"
                Margin="20"
                Opacity="0">
            <Border.RenderTransform>
                <ScaleTransform ScaleX="0.97" ScaleY="0.97"/>
            </Border.RenderTransform>
            <Border.RenderTransformOrigin>0.5,0.5</Border.RenderTransformOrigin>
            <Border.Triggers>
                <EventTrigger RoutedEvent="Loaded">
                    <BeginStoryboard>
                        <Storyboard>
                            <DoubleAnimation Storyboard.TargetProperty="Opacity"
                                             From="0" To="1" Duration="0:0:0.16"/>
                            <DoubleAnimation Storyboard.TargetProperty="RenderTransform.ScaleX"
                                             From="0.97" To="1" Duration="0:0:0.16">
                                <DoubleAnimation.EasingFunction>
                                    <CubicEase EasingMode="EaseOut"/>
                                </DoubleAnimation.EasingFunction>
                            </DoubleAnimation>
                            <DoubleAnimation Storyboard.TargetProperty="RenderTransform.ScaleY"
                                             From="0.97" To="1" Duration="0:0:0.16">
                                <DoubleAnimation.EasingFunction>
                                    <CubicEase EasingMode="EaseOut"/>
                                </DoubleAnimation.EasingFunction>
                            </DoubleAnimation>
                        </Storyboard>
                    </BeginStoryboard>
                </EventTrigger>
            </Border.Triggers>

            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <Border x:Name="IconBorder"
                        Width="52" Height="52"
                        CornerRadius="26"
                        Background="{DynamicResource InfoBackgroundBrush}"
                        BorderBrush="{DynamicResource InfoBorderBrush}"
                        BorderThickness="1"
                        VerticalAlignment="Top"
                        Margin="0,0,16,0">
                    <TextBlock x:Name="IconText"
                               FontSize="23"
                               HorizontalAlignment="Center"
                               VerticalAlignment="Center"/>
                </Border>

                <StackPanel Grid.Column="1">
                    <TextBlock Text="{Binding Titulo}"
                               Style="{StaticResource PopupTitle}"
                               FontSize="19"
                               Margin="0,0,0,8"/>
                    <TextBlock Text="{Binding Mensaje}"
                               Foreground="{DynamicResource TextSecondaryBrush}"
                               FontSize="14"
                               LineHeight="21"
                               TextWrapping="Wrap"
                               MaxWidth="350"/>
                </StackPanel>
            </Grid>
        </Border>

        <Border Grid.Row="1" Style="{StaticResource PopupFooter}">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button x:Name="BtnNegativo"
                        Content="No"
                        Click="OnNegativoClick"
                        MinWidth="96"/>
                <Button x:Name="BtnCancelar"
                        Content="Cancelar"
                        IsCancel="True"
                        Click="OnCancelarClick"
                        MinWidth="96"/>
                <Button x:Name="BtnAfirmativo"
                        Style="{StaticResource PrimaryButton}"
                        Content="Sí"
                        IsDefault="True"
                        Click="OnAfirmativoClick"
                        MinWidth="104"/>
            </StackPanel>
        </Border>
    </Grid>
</Window>
