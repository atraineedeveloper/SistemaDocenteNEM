<UserControl x:Class="SistemaDocente.App.Wpf.Controls.FormField"
             x:Name="RootFormField"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="80" d:DesignWidth="300">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <TextBlock x:Name="HeaderText" Grid.Row="0"
                   Text="{Binding Header, ElementName=RootFormField}"
                   Style="{StaticResource FormLabel}"
                   Margin="0,0,0,4"/>
        <ContentPresenter x:Name="FieldContent" Grid.Row="1"
                          Content="{Binding Content, ElementName=RootFormField}"/>
        <TextBlock x:Name="ErrorText" Grid.Row="2"
                   Text="{Binding Error, ElementName=RootFormField}"
                   Style="{StaticResource Caption}"
                   Foreground="{DynamicResource ErrorBrush}"
                   Visibility="Collapsed"/>
    </Grid>
</UserControl>
