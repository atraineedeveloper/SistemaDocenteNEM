<UserControl x:Class="SistemaDocente.App.Wpf.Controls.EmptyState"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="200" d:DesignWidth="400">
    <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" Margin="{DynamicResource PaddingXLarge}">
        <TextBlock x:Name="IconText" FontSize="36" HorizontalAlignment="Center" Margin="{DynamicResource MarginEmptyStateIcon}"/>
        <TextBlock x:Name="TitleText" Style="{StaticResource Heading3}" HorizontalAlignment="Center" TextAlignment="Center"/>
        <TextBlock x:Name="MessageText" Style="{StaticResource Caption}" HorizontalAlignment="Center" TextAlignment="Center" Margin="{DynamicResource MarginEmptyStateMessage}"/>
        <Button x:Name="ActionButton" Style="{StaticResource PrimaryButton}" Visibility="Collapsed" HorizontalAlignment="Center"/>
    </StackPanel>
</UserControl>
