<UserControl x:Class="SistemaDocente.App.Wpf.Controls.MetricCard"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="60" d:DesignWidth="140">
    <Border Background="{DynamicResource HoverBackgroundBrush}" 
            CornerRadius="{DynamicResource CornerRadiusSmall}" 
            Padding="{DynamicResource PaddingMetricCard}" 
            Margin="{DynamicResource MarginSmall}" 
            BorderBrush="{DynamicResource BorderLightBrush}" 
            BorderThickness="1">
        <TextBlock FontWeight="SemiBold" Foreground="{DynamicResource TextPrimaryBrush}">
            <Run Text="{Binding Title, RelativeSource={RelativeSource AncestorType=UserControl}}"/> 
            <Run Text="{Binding Value, RelativeSource={RelativeSource AncestorType=UserControl}}"/>
        </TextBlock>
    </Border>
</UserControl>
