<Window x:Class="SistemaDocente.App.Wpf.DetalleProyectoWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Detalle del Proyecto Didáctico" Height="620" Width="720" MinHeight="500" MinWidth="580"
        WindowStartupLocation="CenterOwner" ShowInTaskbar="False">
    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVisibility"/>
        <Style TargetType="Button"><Setter Property="Padding" Value="14,7"/><Setter Property="Margin" Value="4"/><Setter Property="MinWidth" Value="100"/></Style>
        <Style TargetType="TextBox"><Setter Property="Padding" Value="8,6"/><Setter Property="Margin" Value="0,4,0,10"/></Style>
    </Window.Resources>
    <Grid Background="#F8FAFC" Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <DockPanel Margin="0,0,0,14">
            <StackPanel DockPanel.Dock="Left">
                <TextBlock Text="Proyecto Didáctico" Foreground="#64748B" FontSize="13" FontWeight="SemiBold"/>
                <TextBlock Text="{Binding NombreProyecto}" FontSize="22" FontWeight="Bold" Foreground="#0F172A"/>
            </StackPanel>
            <Border DockPanel.Dock="Right" CornerRadius="12" Padding="10,4" VerticalAlignment="Center" Background="#E2E8F0">
                <TextBlock Text="{Binding ProyectoSeleccionado.Estado}" FontWeight="SemiBold" Foreground="#334155"/>
            </Border>
        </DockPanel>

        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" Margin="0,0,0,10">
            <StackPanel>
                <Border Background="White" CornerRadius="8" Padding="16" Margin="0,0,0,14" BorderBrush="#E2E8F0" BorderThickness="1">
                    <StackPanel>
                        <TextBlock Text="Información del Proyecto" FontSize="16" FontWeight="SemiBold" Foreground="#1E293B" Margin="0,0,0,10"/>

                        <TextBlock Text="Nombre del Proyecto" FontWeight="SemiBold"/>
                        <TextBox Text="{Binding NombreProyecto, UpdateSourceTrigger=PropertyChanged}"/>

                        <TextBlock Text="Descripción" FontWeight="SemiBold"/>
                        <TextBox Text="{Binding DescripcionProyecto, UpdateSourceTrigger=PropertyChanged}" AcceptsReturn="True" Height="65" TextWrapping="Wrap"/>

                        <Grid Margin="0,0,0,0">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="12"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <StackPanel>
                                <TextBlock Text="Fecha Inicio" FontWeight="SemiBold"/>
                                <TextBox Text="{Binding FechaInicio}"/>
                            </StackPanel>
                            <StackPanel Grid.Column="2">
                                <TextBlock Text="Fecha Término" FontWeight="SemiBold"/>
                                <TextBox Text="{Binding FechaTermino}"/>
                            </StackPanel>
                        </Grid>

                        <TextBlock Foreground="#B45309" Text="⚠️ Duración fuera del rango estándar (14–31 días). Puedes guardar si es intencional."
                                   Visibility="{Binding DuracionAtipica, Converter={StaticResource BoolToVisibility}}" Margin="0,0,0,8"/>

                        <TextBlock Text="Observaciones" FontWeight="SemiBold"/>
                        <TextBox Text="{Binding ObservacionesProyecto, UpdateSourceTrigger=PropertyChanged}" AcceptsReturn="True" Height="55" TextWrapping="Wrap"/>

                        <WrapPanel Margin="0,6,0,0">
                            <Button Content="Guardar cambios" Command="{Binding GuardarProyectoCommand}"/>
                            <Button Content="Iniciar" Command="{Binding IniciarProyectoCommand}"/>
                            <Button Content="Finalizar" Command="{Binding FinalizarProyectoCommand}"/>
                            <Button Content="Reabrir" Command="{Binding ReabrirProyectoCommand}"/>
                            <Button Content="Eliminar borrador" Command="{Binding EliminarProyectoCommand}"/>
                        </WrapPanel>
                    </StackPanel>
                </Border>

                <Border Background="White" CornerRadius="8" Padding="16" BorderBrush="#E2E8F0" BorderThickness="1">
                    <StackPanel>
                        <DockPanel Margin="0,0,0,10">
                            <TextBlock DockPanel.Dock="Left" Text="Actividades del Proyecto" FontSize="16" FontWeight="SemiBold" Foreground="#1E293B" VerticalAlignment="Center"/>
                            <Button DockPanel.Dock="Right" Content="+ Nueva Actividad" Click="OnNuevaActividadClic"/>
                        </DockPanel>

                        <TextBox Text="{Binding BusquedaActividad, UpdateSourceTrigger=PropertyChanged}" ToolTip="Buscar actividad..."/>

                        <ListBox x:Name="ListaActividades" Height="170" ItemsSource="{Binding Actividades}" SelectedItem="{Binding ActividadSeleccionada}" MouseDoubleClick="OnActividadDobleClic">
                            <ListBox.ItemTemplate>
                                <DataTemplate>
                                    <DockPanel Margin="4">
                                        <TextBlock DockPanel.Dock="Right" Text="{Binding Estado}" Foreground="#64748B" VerticalAlignment="Center"/>
                                        <StackPanel DockPanel.Dock="Left">
                                            <TextBlock Text="{Binding Titulo}" FontWeight="SemiBold" FontSize="14"/>
                                            <TextBlock Text="{Binding FechaRealizacion, StringFormat=Fecha: {0:dd/MM/yyyy}}" Foreground="#475569" FontSize="12"/>
                                        </StackPanel>
                                    </DockPanel>
                                </DataTemplate>
                            </ListBox.ItemTemplate>
                        </ListBox>
                        
                        <WrapPanel Margin="0,8,0,0" HorizontalAlignment="Right">
                            <Button Content="Editar actividad seleccionada" Click="OnEditarActividadClic"/>
                        </WrapPanel>
                    </StackPanel>
                </Border>
            </StackPanel>
        </ScrollViewer>

        <DockPanel Grid.Row="2" Margin="0,4,0,0">
            <Button DockPanel.Dock="Right" Content="Cerrar" Click="OnCerrarClic" IsCancel="True"/>
        </DockPanel>
    </Grid>
</Window>
